# Mini HCM

Firebase Auth + Firestore, React (Vite), Express + firebase-admin.

**Setup:** read `SETUP.md`, then from this folder run:

Run from this folder:

```
npm install
npm run install:all
npm run dev
```
